{
  "name": "koreamate-workspace",
  "private": true,
  "version": "3.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm run dev -w @koreamate/web\" \"npm run dev -w @koreamate/api\"",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "legacy:dev": "concurrently -n frontend,backend -c yellow,blue \"npm --prefix frontend run dev\" \"npm --prefix backend run start:dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
